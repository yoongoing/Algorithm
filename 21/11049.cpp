#include <iostream>
#include <vector>

using namespace std;

int N;
vector<pair<int, int> > v;

int main(void){
    ios::sync_with_stdio(false);
    cin.tie(0);

    cin>>N;
    for(int i=0; i<N; i++){
        int r,c;
        cin>>r>>c;
        v.push_back(make_pair(r,c));
        for
    }
        
    return 0;
}